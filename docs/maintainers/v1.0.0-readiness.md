# v1.0.0 Readiness

This document tracks the validation work needed before intentionally signaling
`v1.0.0` for `octostate`.

Use it as the source of truth for the readiness branch and readiness PR. The
actual version bump, changelog update, tag, and GitHub Release should still be
created by `release-please` after the readiness PR lands on `main`.

## Goals

- prove that the renamed `octostate` CLI is stable across core user flows
- capture reproducible evidence for the release-readiness PR
- surface and fix defects before intentionally triggering the first stable
  release

## Rules Of Evidence

- Use a sandbox GitHub org and disposable repositories only.
- Prefer reproducible command logs and CI artifacts over screenshots.
- Use screenshots only as supporting evidence when they improve readability.
- Record exact commands, fixtures, expected results, and actual results.
- If validation finds a defect, fix it and rerun the affected checks before
  marking the scenario complete.

## Readiness Workflow

1. Create a dedicated readiness branch and draft PR linked to the tracking
   issue.
2. Fill in the environment summary before running live validations.
3. Work through the validation checklist below and attach evidence as you go.
4. Fix any defects on the readiness branch, then rerun the affected scenarios.
5. When every required scenario is green, convert the PR from draft to ready.
6. Merge the readiness PR with an intentional releasable and breaking
   Conventional Commit signal only when you are ready for `release-please` to
   open or update the `v1.0.0` release PR.

## Environment Summary

- Date: 2026-05-11T14:34:42Z
- Operator: Codex, using FerdiHS-local credentials
- OS: macOS Darwin 25.4.0 arm64
- Go version: go1.25.6 darwin/arm64
- Auth method: GitHub App for mock-org live validation; `CODEX_GITHUB_PAT`
  for PR updates
- Sandbox org: `orang-gaboets-test`
- Test repositories: `empty`, `empty-apply-test`, `empty-template`
- Notes: live validation used ignored local config `out/organization.yaml`;
  refreshed audit state was written to
  `/private/tmp/octostate-readiness-state/actual/snapshot.json`

## Validation Checklist

### Build And Install

- [x] `go build -o bin/octostate ./cmd/octostate`
- [x] `go run ./cmd/octostate --help`
- [x] install/build/run examples in tracked docs still match the current CLI

### Offline GitOps

- [x] `octostate config validate` succeeds for a known-good config
- [x] `octostate config validate` fails clearly for an intentionally bad config
- [x] `octostate audit diff` behaves as expected against prepared snapshots

### Live GitOps

- [x] `octostate config plan` succeeds against sandbox live state
- [x] `octostate config apply --dry-run` reports the expected changes
- [x] `octostate config apply` converges the target state successfully
- [x] idempotency check passes: after apply, a second `config plan` shows no
      remaining actionable changes
- [x] `octostate audit pull` succeeds against sandbox live state
- [x] drift check passes: after pull, `octostate audit diff` shows no
      unexpected drift

### Sync From Live

- [x] `octostate config sync-from-live bootstrap` works
- [x] `octostate config sync-from-live adopt` works
- [x] `octostate config sync-from-live materialize` works

### Primitive CLI Live Smoke

- [x] read-only organization commands: `get-by-name`, `list-repos`,
      `list-members`, `list-teams`, `list-invitations`
- [x] user commands: `get-by-username`, `get-by-id`
- [x] repository lifecycle: `create-from-template`, `get`, `edit`, `delete`
- [x] topic lifecycle: `list`, `add`, `replace`
- [x] team lifecycle: `create`, `get-by-slug`, `edit`, `delete-by-slug`
- [x] team member lifecycle: `list`, `add`, `remove`
- [x] team repository permissions lifecycle: `list`, `add`, `remove`
- [x] real organization invite: invite, verify pending invitation, cancel
      invitation, and verify cleanup

### Config Apply Real Write

- [x] `octostate config apply` executes a real repository update against a
      disposable smoke repository
- [x] baseline config restore returns the mock org to a clean plan and clean
      audit diff

### Negative Coverage Audit

- [x] command bad-path coverage reviewed across every root command and
      subcommand
- [x] live smoke evidence is treated as happy-path coverage, not exhaustive
      command testing
- [x] automated tests cover representative bad paths: required flags,
      authentication errors, invalid values, dry-run behavior, destructive
      confirmation, dependency errors, and typed exit behavior
- [x] no intentionally failing live GitHub scenarios were added

### Rename And Migration

- [x] user-facing docs, help text, install paths, and examples consistently use
      `octostate`
- [x] migration guidance from `repo-builder` to `octostate` is clear enough for
      existing users

### Repository Health

- [x] `go test ./...`
- [x] `go vet ./...`
- [x] `golangci-lint run --timeout=5m`

## Evidence Log

Use one entry per meaningful validation pass. Link to CI runs, attach terminal
logs, or paste concise command excerpts in the readiness PR.

### Entry Template

- Date:
- Operator:
- Area:
- Commands:
- Fixtures or repos:
- Expected result:
- Actual result:
- Artifacts:
- Follow-up:

### 2026-05-11 Read-Only Validation Pass

- Date: 2026-05-11T14:34:42Z
- Operator: Codex
- Area: build, offline GitOps, read-only live GitOps, sync-from-live, repo
  health
- Commands:
  - `go build -o bin/octostate ./cmd/octostate`
  - `go run ./cmd/octostate --help`
  - `bin/octostate config validate --config-dir pkg/gitops/config/testdata/valid/minimal`
  - `bin/octostate config validate --config-dir pkg/gitops/config/testdata/invalid/unknown-field`
  - `bin/octostate audit pull --config-dir out --state-dir /private/tmp/octostate-readiness-state --app-id <redacted> --installation-id <redacted> --app-key-path env/private-key.pem`
  - `bin/octostate audit diff --config-dir out --state-dir /private/tmp/octostate-readiness-state`
  - `bin/octostate config plan --config-dir out --app-id <redacted> --installation-id <redacted> --app-key-path env/private-key.pem`
  - `bin/octostate config apply --config-dir out --dry-run --app-id <redacted> --installation-id <redacted> --app-key-path env/private-key.pem`
  - `bin/octostate config sync-from-live --mode bootstrap --org orang-gaboets-test --config-dir /private/tmp/octostate-sync-bootstrap --app-id <redacted> --installation-id <redacted> --app-key-path env/private-key.pem`
  - `bin/octostate config sync-from-live --mode adopt --org orang-gaboets-test --config-dir out --app-id <redacted> --installation-id <redacted> --app-key-path env/private-key.pem`
  - `bin/octostate config sync-from-live --mode materialize --org orang-gaboets-test --config-dir out --app-id <redacted> --installation-id <redacted> --app-key-path env/private-key.pem`
  - `go test ./...`
  - `go vet ./...`
  - `golangci-lint run --timeout=5m`
- Fixtures or repos: `pkg/gitops/config/testdata/valid/minimal`,
  `pkg/gitops/config/testdata/invalid/unknown-field`, ignored local
  `out/organization.yaml`, sandbox repositories `empty`, `empty-apply-test`,
  and `empty-template`
- Expected result: build/help, validation, read-only live collection, dry-run,
  sync generation, and repo health checks complete without unexpected errors
- Actual result: all listed commands passed except the intentionally invalid
  config validation, which failed clearly with an unknown-field decode error
- Artifacts: PR evidence comment for this validation pass
- Follow-up: run mutating `config apply`, idempotency, and clean drift checks
  after confirming the sandbox desired state should be converged

### 2026-05-11 Convergence Validation Pass

- Date: 2026-05-11T14:56:08Z
- Operator: Codex
- Area: live GitOps convergence, idempotency, clean drift, migration guidance
- Commands:
  - `bin/octostate config sync-from-live --mode bootstrap --org orang-gaboets-test --config-dir /private/tmp/octostate-readiness-bootstrap-config --write --app-id <redacted> --installation-id <redacted> --app-key-path env/private-key.pem`
  - `bin/octostate config validate --config-dir /private/tmp/octostate-readiness-bootstrap-config`
  - `bin/octostate config plan --config-dir /private/tmp/octostate-readiness-bootstrap-config --app-id <redacted> --installation-id <redacted> --app-key-path env/private-key.pem`
  - `bin/octostate config apply --config-dir /private/tmp/octostate-readiness-bootstrap-config --dry-run --app-id <redacted> --installation-id <redacted> --app-key-path env/private-key.pem`
  - `bin/octostate config apply --config-dir /private/tmp/octostate-readiness-bootstrap-config --app-id <redacted> --installation-id <redacted> --app-key-path env/private-key.pem`
  - `bin/octostate config plan --config-dir /private/tmp/octostate-readiness-bootstrap-config --app-id <redacted> --installation-id <redacted> --app-key-path env/private-key.pem`
  - `bin/octostate audit pull --config-dir /private/tmp/octostate-readiness-bootstrap-config --state-dir /private/tmp/octostate-readiness-clean-state --app-id <redacted> --installation-id <redacted> --app-key-path env/private-key.pem`
  - `bin/octostate audit diff --config-dir /private/tmp/octostate-readiness-bootstrap-config --state-dir /private/tmp/octostate-readiness-clean-state`
  - `rg -n --hidden --glob '!.git/**' --glob '!*.local.*' 'repo-builder|cmd/repo-builder|github.com/orang-gaboets/repo-builder'`
- Fixtures or repos: generated temporary bootstrap config,
  `orang-gaboets-test`, `empty`, `empty-apply-test`, and `empty-template`
- Expected result: generated desired state matches live state, apply succeeds,
  post-apply plan remains clean, and audit diff reports no drift
- Actual result: bootstrap config validated with 3 repositories, 2 members,
  1 team, and 0 invites; plan, dry-run, actual apply, post-apply plan, and
  audit diff all reported 0 actions and `has_changes: false`
- Artifacts: PR evidence comment for this validation pass
- Follow-up: if release readiness requires proof of a write mutation, run a
  separate sandbox mutation-and-rollback pass before converting this PR from
  draft to ready

### 2026-05-12 Primitive CLI Live Smoke Pass

- Date: 2026-05-12T15:03:05Z
- Operator: Codex
- Area: primitive CLI command families, real GitOps write, cleanup verification
- Commands:
  - `go build -o bin/octostate ./cmd/octostate`
  - `bin/octostate organization get-by-name --org orang-gaboets-test ...`
  - `bin/octostate organization list-repos --org orang-gaboets-test ...`
  - `bin/octostate organization list-members --org orang-gaboets-test ...`
  - `bin/octostate organization list-teams --org orang-gaboets-test ...`
  - `bin/octostate organization list-invitations --org orang-gaboets-test ...`
  - `bin/octostate user get-by-username --username gaboets-test ...`
  - `bin/octostate user get-by-id --id 226546627 ...`
  - `bin/octostate repo create-from-template --org orang-gaboets-test --template-name empty-template --name octostate-v1-smoke-20260512145149 ...`
  - `bin/octostate repo get --org orang-gaboets-test --name octostate-v1-smoke-20260512145149 ...`
  - `bin/octostate repo edit --org orang-gaboets-test --name octostate-v1-smoke-20260512145149 ...`
  - `bin/octostate topic list/add/replace --org orang-gaboets-test --name octostate-v1-smoke-20260512145149 ...`
  - `bin/octostate team create/get-by-slug/edit --org orang-gaboets-test --slug octostate-v1-smoke-20260512145149 ...`
  - `bin/octostate team members list/add/remove --org orang-gaboets-test --slug octostate-v1-smoke-20260512145149 ...`
  - `bin/octostate team repo permissions list/add/remove --org orang-gaboets-test --slug octostate-v1-smoke-20260512145149 --repo octostate-v1-smoke-20260512145149 ...`
  - `bin/octostate config sync-from-live --mode bootstrap --org orang-gaboets-test --write ...`
  - `bin/octostate config apply --config-dir <mutated temp config> ...`
  - `bin/octostate config apply --config-dir <baseline temp config> ...`
  - `bin/octostate audit pull/diff --config-dir <baseline temp config> ...`
  - `bin/octostate team delete-by-slug --org orang-gaboets-test --slug octostate-v1-smoke-20260512145149 --yes ...`
  - `bin/octostate repo delete --org orang-gaboets-test --name octostate-v1-smoke-20260512145149 --yes ...`
  - final `config sync-from-live`, `config validate`, `config plan`,
    `audit pull`, and `audit diff`
- Fixtures or repos: generated smoke repository
  `octostate-v1-smoke-20260512145149`, generated smoke team
  `octostate-v1-smoke-20260512145149`, template repository `empty-template`,
  and team member `gaboets-test`
- Expected result: each primitive command family succeeds against disposable
  resources, real GitOps apply performs a write, cleanup removes smoke
  resources, and final plan/diff are clean
- Actual result: all non-invite primitive command families passed; real
  `config apply` executed 1 repository update; baseline restore and final
  cleanup verification ended with `has_changes: false` and 0 audit diff
  actions
- Artifacts: local log directory
  `/private/tmp/octostate-v1-primitive-smoke-20260512145149`; PR evidence
  comment for this validation pass
- Follow-up: completed in the 2026-05-12 organization invite smoke pass

### 2026-05-12 Organization Invite Smoke Pass

- Date: 2026-05-12T22:01:50Z
- Operator: Codex
- Area: real organization invitation path and cleanup verification
- Commands:
  - `go build -o bin/octostate ./cmd/octostate`
  - `bin/octostate user get-by-username --username gaboets-test ...`
  - `bin/octostate organization list-members --org orang-gaboets-test ...`
  - `bin/octostate organization list-invitations --org orang-gaboets-test ...`
  - `bin/octostate organization invite --org orang-gaboets-test --username gaboets-test ...`
  - `bin/octostate organization list-invitations --org orang-gaboets-test ...`
  - `DELETE /orgs/orang-gaboets-test/invitations/75351545`
  - final `organization list-invitations` and `organization list-members`
- Fixtures or repos: sandbox organization `orang-gaboets-test`, non-member
  test account `gaboets-test`, canceled invitation `75351545`
- Expected result: `gaboets-test` is absent from organization members before
  the invite, `organization invite` creates a pending invitation, the pending
  invitation is visible, REST cancellation succeeds, and final member/invite
  checks are clean
- Actual result: `organization invite` returned success for user ID
  `226546627`; `organization list-invitations` showed pending invitation
  `75351545` for `gaboets-test`; cancellation returned HTTP `204`; final
  invitation and member checks both returned 0 matches for `gaboets-test`
- Artifacts: local log directory
  `/private/tmp/octostate-v1-invite-smoke-20260512220150`; PR evidence comment
  for this validation pass
- Follow-up: none for this command path; `gaboets-test` remains outside the
  sandbox organization after the canceled invitation

### 2026-05-12 Negative Coverage Audit Pass

- Date: 2026-05-12T23:09:20Z
- Operator: Codex
- Area: automated bad-path coverage for command readiness
- Commands:
  - `find cmd/octostate -name '*_test.go' -type f`
  - `rg -n 'ErrNoValidCredentials|ErrConflictingCredentials|ErrMissingRequiredField|ErrInvalidFieldValue|ErrValidationFailed|ErrConfirmationRequired|required flag|invalid flag|dry-run|service error|not found' cmd/octostate -g '*_test.go'`
  - `go test ./cmd/octostate/...`
  - `go test ./...`
  - `go vet ./...`
  - `golangci-lint run --timeout=5m`
- Fixtures or repos: local unit tests and mock GitHub services only; no live
  negative GitHub scenarios
- Coverage decisions:
  - root/main: existing tests cover verbose output separation, typed exit
    handling, regular error handling, and Cobra required-flag stderr behavior
  - config/audit: existing tests cover load failures, semantic validation,
    auth/collector/planner/apply/snapshot failures, typed exit behavior, and
    write-failure cleanup paths
  - organization primitives: existing tests cover required flags, auth errors,
    invalid role/identity input, dry-run invite behavior, and output shape; new
    tests cover dependency-error propagation for get, invite, list-members,
    list-repos, list-teams, and list-invitations
  - user primitives: existing tests cover required flags, auth errors, invalid
    flags, and output shape; new tests cover dependency-error propagation for
    username and ID lookup
  - repo/topic/team primitives: existing tests cover required flags, auth
    errors, invalid values, dry-run behavior, destructive confirmation where
    applicable, and output shape; new tests cover dependency-error propagation
    for each primitive command
  - aliases/flag permutations: not exhaustively tested because aliases share
    Cobra command implementations; only aliases with distinct behavior need
    targeted tests
- Expected result: the audit should not rely on live smoke for bad paths, and
  any material command-level dependency-error gaps should be covered with
  focused unit tests
- Actual result: added focused service-error tests for primitive command
  dependency failures; retained existing config/audit bad-path coverage; final
  repository health checks passed
- Artifacts: PR evidence comment for this validation pass
- Follow-up: none for v1 readiness; future command additions should include
  representative bad-path tests alongside happy-path tests

## Migration Guidance Draft

Use this guidance in the eventual `v1.0.0` release PR and release notes:

- Update Go imports from `github.com/orang-gaboets/repo-builder/...` to
  `github.com/orang-gaboets/octostate/...`.
- Update install commands from
  `go install github.com/orang-gaboets/repo-builder/cmd/repo-builder@latest`
  to `go install github.com/orang-gaboets/octostate/cmd/octostate@latest`.
- Update local build and run commands from `cmd/repo-builder` to
  `cmd/octostate`.
- Update CLI invocations from `repo-builder ...` to `octostate ...`.
- No compatibility alias for the old CLI name is currently planned.

## Exit Criteria

- Every required checklist item above is complete.
- Defects found during validation are fixed or moved to explicit follow-up
  issues with a conscious decision not to block the release.
- The readiness PR contains enough evidence for another maintainer to audit the
  release decision without rerunning every flow from scratch.
- The merge strategy and final PR title are chosen intentionally so
  `release-please` opens or updates the `v1.0.0` release PR only when you want
  it to.
