version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - staticcheck
    - revive
    - gocyclo

  settings:
    errcheck:
      check-type-assertions: true
      check-blank: true
      exclude-functions:
        - 'fmt\.Print.*'
        - 'fmt\.Fprintf'
