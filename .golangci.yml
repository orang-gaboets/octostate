version: 2

run:
  timeout: 5m

linters:
  enable:
    - staticcheck
    - revive
    - gocyclo

linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: true
    exclude-functions:
      - 'fmt\.Print.*'
      - 'fmt\.Fprintf'
